#include "main.h"
#include <stdio.h>

/**
 * main - MK : prints the name of a program
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	_putchar("%s\n", argv[argc * 0]);

	return (0);
}
