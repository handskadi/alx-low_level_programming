#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * main - MK prints the name of program
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
